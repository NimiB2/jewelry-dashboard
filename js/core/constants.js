// Core constants placeholder. Keep logic unchanged.
// When migrating, move constant values from app.js here and reference via global App namespace.

window.App = window.App || {};
window.App.Constants = {
  // category labels, statuses, etc. will be placed here during migration
};
