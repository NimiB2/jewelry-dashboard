// DOM helpers placeholder (query, setText, setHTML, toggle, etc.).

window.App = window.App || {};
window.App.Utils = window.App.Utils || {};
window.App.Utils.Dom = {
  // q: (sel, root=document) => root.querySelector(sel),
};
